package app.opencv;

import org.opencv.imgproc.Imgproc;
import org.opencv.ml.KNearest;

public class NearestNeighbour {

    public void findNearestNeighbour(){
        //KNearest knn = new KNearest(trainingData, trainingClasses, cv.Mat(), false, K));
    }
}
